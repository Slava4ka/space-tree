# 3D Nodes Viewer

Интерактивная 3D визуализация сетки нод с анимацией камеры и видео-фоном.

## Возможности

- 🎯 Сетка из круглых нод (сфер) в 3D пространстве
- 📝 3D текст под каждой нодой с описанием
- 🎬 Видео-фон на всю сцену
- 🎨 Плавная анимация камеры при клике на ноду
- ✨ Увеличение выбранной ноды
- 🖱️ Интерактивность (hover эффекты, курсор)

## Технологии

- React 18
- TypeScript
- Three.js
- @react-three/fiber
- @react-three/drei
- GSAP (для анимаций)
- Vite

## Установка

```bash
cd nodes-3d-viewer
pnpm install
```

## Запуск

```bash
pnpm dev
```

Проект откроется на `http://localhost:5173`

## Структура проекта

```
nodes-3d-viewer/
├── src/
│   ├── components/
│   │   ├── Scene3D.tsx      # Основная 3D сцена
│   │   ├── NodeSphere.tsx   # Компонент ноды (сфера)
│   │   └── VideoBackground.tsx # Видео-фон
│   ├── data/
│   │   └── nodes.ts         # Моки данных нод
│   ├── App.tsx
│   ├── main.tsx
│   └── styles.css
├── package.json
└── vite.config.ts
```

## Использование

1. При загрузке центральная нода уже выбрана
2. Кликните на любую ноду для переключения
3. Камера плавно переместится к выбранной ноде
4. Выбранная нода увеличится в размере

## Настройка

Данные нод находятся в `src/data/nodes.ts`. Вы можете изменить:
- Позиции нод
- Тексты описаний
- Цвета нод

Для замены видео-фона измените URL в `src/components/VideoBackground.tsx`.

