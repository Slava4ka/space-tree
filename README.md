# Научно-технологические векторы России

Интерактивная 3D визуализация научно-технологических векторов России с использованием Three.js.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск проекта

#### Режим разработки (с hot-reload)
```bash
npm run dev
```

Проект автоматически откроется в браузере на `http://localhost:8000`

#### Сборка для продакшена
```bash
npm run build
```

Собранные файлы будут в папке `dist/`

#### Просмотр собранной версии
```bash
npm run preview
```

---

### Альтернативный способ (без npm)

Если вы не хотите использовать npm, можно использовать простой HTTP-сервер:

```bash
# Python 3
python3 -m http.server 8000

# Или Node.js (http-server)
npx http-server -p 8000
```

**Примечание:** При использовании простого HTTP-сервера нужно вернуть CDN-подключения в `index.html` (см. историю коммитов).

## 📁 Структура проекта

```
graph2/
├── index.html          # Основная HTML страница
├── styles.css          # Стили приложения
├── src/
│   ├── main.js        # Главный скрипт инициализации Three.js
│   ├── OrbitText.js   # Класс для орбитальных объектов с подписями
│   └── particles.js   # Генерация и управление фоновыми частицами
└── assets/            # Папка для ресурсов (текстуры, шрифты)
```

## ✨ Особенности

- **Центральная сфера** с эффектом свечения (Bloom)
- **8 орбитальных сфер** с подписями, вращающихся вокруг центра
- **Фоновые частицы** (звёздное небо) с эффектом параллакса
- **Интерактивность**: наведение и клик по сферам
- **Адаптивный дизайн** для разных размеров экрана
- **Плавные анимации** через GSAP
- **Автоматическое вращение** камеры и объектов

## 🎮 Управление

- **Наведение мыши** на орбитальную сферу — увеличение и подсветка
- **Клик** по сфере — открытие модального окна с описанием
- **Движение мыши** — эффект параллакса для фоновых частиц

## 🛠 Технологии

- **Three.js** (v0.160.0) - 3D графика
- **GSAP** (v3.12.5) - анимации
- **Vite** (v5.0.0) - сборщик и dev-сервер
- **CSS2DRenderer** - подписи поверх 3D сцены
- **UnrealBloomPass** - эффект свечения (Bloom)

## 📝 Настройка

Все параметры можно изменить в `src/main.js`:

- Количество орбитальных сфер: массив `orbitData`
- Радиус орбиты: переменная `orbitRadius`
- Скорость вращения: `autoRotateSpeed`
- Количество частиц: параметр `count` в `createParticles()`

